# CDM Terraform resource support may vary. Validate against the active provider.
# If unsupported, create CDM manually or through DataArts Studio/API.

# TODO: Replace with provider-supported CDM resource when available.
# resource "huaweicloud_cdm_cluster" "this" {
#   name              = "${var.name_prefix}-cdm"
#   vpc_id            = var.vpc_id
#   subnet_id         = var.subnet_id
#   security_group_id = var.security_group_id
# }

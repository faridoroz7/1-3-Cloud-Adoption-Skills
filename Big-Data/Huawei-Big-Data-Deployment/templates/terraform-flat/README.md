# Terraform Flat Template

This folder is intentionally minimal. Use it when a user asks for a simple single-folder deployment instead of reusable modules.

Recommended files to generate:

```text
versions.tf
provider.tf
variables.tf
main.tf
outputs.tf
terraform.tfvars.example
```

For this skill, modular Terraform is preferred.
